// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("phonenumber", "for phonenumber support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("phonenumber", "enable phonenumber support", "no");

if (PHP_PHONENUMBER != "no") {
	EXTENSION("phonenumber", "phonenumber.c");
}

